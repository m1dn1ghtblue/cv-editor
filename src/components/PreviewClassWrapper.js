import React, { Component } from 'react';
import Preview from './CV/Preview';

class PreviewClassWrapper extends Component {
	render() {
		return <Preview />;
	}
}

export default PreviewClassWrapper;
